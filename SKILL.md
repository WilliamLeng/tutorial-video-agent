---
name: tutorial-video
description: 将带自然讲解的手机或电脑录屏整理为可发布的操作教程。用于分析录屏、清理口播、拆分课程步骤、匹配操作画面、生成新解说稿、制定点击提示和自动剪辑决策，以及在用户不参与中间审核时保守地生成成片。
---

# 录屏操作教程

## 核心原则

- 把原录音作为内容与时间证据，不直接作为最终解说音轨。
- 同时依据原始转写和实际画面，不仅依据其中一种。
- 不补充证据中不存在的业务规则、权限、条件或注意事项。
- 无法确定时采用保守方案：少删、少标、保留操作结果。
- 用户要求直接看成片时，执行内部校验，不把待确认事项转交用户。

## 工作流

1. 读取 `media.json`，确认时长、画面方向、帧率和音轨。
2. 完整读取 `transcript.raw.json`，不得只看摘要。
3. 读取 `frame-index.json`，按时间顺序查看全部关键帧。
4. 建立“原讲解—页面—操作—结果”证据链。
5. 读取 [课程草稿规则](references/draft-rules.md)，生成 `course-draft.json`。
6. 运行项目的数据模型和业务校验。
7. 读取 [自动验收规则](references/review-rules.md)，修正高风险问题。
8. 生成 `course-draft-review.md` 和 `validation-notes.md`。
9. 根据草稿生成 `render-project.json`，格式参考项目中的 `examples/render-project.example.json`。
10. 运行 `tutorial-video render render-project.json`，生成新配音、字幕、点击提示、背景音乐和成片。
11. 检查同名 manifest 中的实际变速，确认每个普通操作不超过 `1.5x`。

## 草稿要求

每个步骤必须包含：

- 步骤标题；
- 完善后的解说句；
- 原视频绝对时间范围；
- 页面、口播或画面变化证据；
- 置信度；
- 是否需要内部保守处理；
- 点击文字和操作结果（存在时）。

不得覆盖原始转写或关键帧索引。

## 直接成片模式

用户不参与审核时：

- 自动修正可由证据判定的问题；
- 删除明显无关页面、重复尝试和长等待；
- 不删除疑似关键操作；
- 点击位置不确定时省略点击标注，不制造错误指引；
- 注意事项缺乏证据时不生成该章节；
- 成片前检查操作顺序、音画同步、字幕遮挡和结尾完整性。
- 需要压缩等待时优先把一个步骤拆成多个有效 `clips`，不要直接把完整长片段高速播放。
- 点击和隐私区域使用相对于原画面的比例坐标，必须以对应时间点截图为依据。

## 完成条件

只有同时满足以下条件才能交付：

- 新解说没有改变功能含义；
- 关键操作和结果均被保留；
- 点击效果没有指向错误区域；
- 字幕与新解说同步；
- 背景音乐不影响可懂度；
- 输出视频可完整播放；
- 未确认业务内容没有被写成确定事实。
