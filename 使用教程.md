# 使用教程

## 一、准备环境

确认电脑已安装 Python、FFmpeg 和 FFprobe：

```bash
python3 --version
ffmpeg -version
ffprobe -version
```

创建独立环境并安装项目：

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[local-asr]"
```

## 二、准备录屏

建议录制时做到：

- 一边操作一边讲清楚操作目的；
- 明确说出页面、按钮和字段名称；
- 操作完成后停留片刻，让最终结果进入画面；
- 出错时重新完整演示正确步骤；
- 尽量避免显示真实姓名、联系方式和客户数据。

录屏不需要像正式课程一样流畅。口头语、重复、等待和试错可在后续流程中处理，但讲解必须能帮助系统理解操作目的。

## 三、生成分析材料

```bash
tutorial-video prepare source.mp4 --output outputs/demo
```

完成后先检查：

- `media.json` 中的视频信息是否正确；
- `transcript.raw.json` 是否覆盖完整讲解；
- `frame-index.json` 和 `frames/` 是否覆盖主要页面变化。

## 四、生成课程结构

让支持本项目 Skill 的 Agent 读取：

1. 根目录的 `SKILL.md`；
2. `media.json`；
3. 完整的 `transcript.raw.json`；
4. `frame-index.json` 和全部关键帧。

Agent 会生成 `course-draft.json`，并保留每个步骤的证据和原视频范围。生成后运行：

```bash
tutorial-video validate-draft course-draft.json --media outputs/demo/media.json
```

## 五、准备配音

### 方式 A：macOS 系统语音

在渲染配置中设置：

```json
{
  "voice_provider": "macos",
  "voice": "Tingting",
  "voice_rate": 175
}
```

### 方式 B：其他本地语音模型

分别生成片头和每个课程步骤的音频，并按照课程顺序写入 `voice_files`。音频数量必须等于“片头数量 1 + 步骤数量”。

## 六、配置画面

复制 `examples/render-project.example.json`，填写：

- 原始视频和输出视频路径；
- 片头标题、旁白和背景截图时间；
- 每个步骤使用的原视频片段；
- 点击、聚焦、遮挡和转场效果；
- 背景音乐文件；
- 配音方式和相关文件。

效果坐标为原画面的相对比例，范围为 `0` 到 `1`。例如 `x=0.5` 表示从画面宽度的中点开始。

## 七、渲染与检查

```bash
tutorial-video render render-project.json
```

渲染完成后不要只看日志。必须播放最终视频并检查：

- 是否存在错页、错字或错误点击框；
- 旁白开始时是否已进入对应页面；
- 每个结果是否有足够停留时间；
- 字幕是否遮挡按钮；
- 隐私信息是否全部处理；
- 背景音乐是否影响讲解；
- 最终文件是否能够完整播放。

## 八、成片后优化

当前推荐采用“先生成完整成片，再统一优化”的工作方式。人工重点调整内容准确性、点击坐标、字幕安全区、节奏和视觉观感，不需要在每个中间环节逐项确认。
