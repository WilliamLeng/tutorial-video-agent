# 能力边界

## 可以自动完成

- 读取视频媒体信息；
- 提取音轨并运行本地语音识别；
- 按固定间隔提取关键帧；
- 保存带时间范围和证据的课程结构；
- 校验步骤是否超出原视频范围；
- 根据配音时长重建时间线；
- 生成字幕和常用操作提示；
- 合成配音、音乐和画面；
- 检查导出视频的基本媒体属性。

## 需要 Agent 判断

- 哪些内容是真正的教学目标；
- 哪些口语、试错和等待可以删除；
- 原讲解与页面不一致时如何保守处理；
- 如何将真实操作拆成易学的步骤；
- 哪些页面变化能够证明操作结果；
- 是否需要点击提示、停帧或隐私遮挡。

## 仍需人工最终负责

- 业务内容是否准确；
- 页面是否属于最新产品版本；
- 复杂控件的准确坐标；
- 客户数据和个人信息是否完全脱敏；
- 课程节奏、音色和视觉效果是否适合目标学员；
- 最终视频是否符合组织的发布规范。

## 当前不应宣传为

- 任意视频都能一次生成完美课程；
- 完全不需要人工判断；
- 已封装为面向普通用户的一键桌面软件；
- 已适配所有操作系统和硬件；
- 自带可自由商用的模型、音色、音乐和字体。

## 已知环境限制

默认本地语音识别基于 MLX，主要面向 Apple Silicon。Windows 和 Linux 使用者需要替换转写实现。默认系统语音也依赖 macOS；其他系统可以通过 `voice_provider=files` 提供本地生成的音频。
