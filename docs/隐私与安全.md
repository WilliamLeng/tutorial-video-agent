# 隐私与安全

## 数据来源

程序不预置业务数据。被分析的语音、页面、操作和结果均来自使用者主动导入的原始录屏。

本地处理并不自动等于没有隐私风险。原始录屏、关键帧、转写文本、配音文件和最终视频都可能包含敏感信息，因此必须对完整处理链进行管理。

## 默认安全原则

- 不覆盖或删除原始视频；
- 不把原始素材提交到代码仓库；
- 模型和临时输出放在被 `.gitignore` 排除的目录；
- 点击位置和业务规则必须来自录屏证据；
- 无法确认的内容不写成确定结论；
- 成片发布前进行人工隐私检查。

## 发布前检查清单

- 姓名、手机号、头像和账号；
- 客户资料、聊天记录和订单；
- 公司内部地址、编号和系统入口；
- 密钥、口令、验证码和访问令牌；
- 文件路径、设备名称和通知内容；
- 语音中提到但画面没有展示的敏感信息；
- 背景音乐、字体、音色和示例素材的授权范围。

## 模型下载说明

首次使用本地语音识别模型时，相关工具可能需要联网下载模型文件。希望在隔离环境中运行的使用者，应提前准备模型并确认缓存位置。项目本身不上传录屏，但第三方依赖的实际行为仍应由使用者根据其版本和文档单独核查。
