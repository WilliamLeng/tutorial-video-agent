# 工作原理

## 1. 输入不是普通素材，而是多模态操作证据

原始录屏同时包含三类信息：

- 讲解者说了什么；
- 屏幕处于哪个页面、出现了哪些控件；
- 点击、输入、选择和提交后，页面发生了什么变化。

系统将语音时间戳与关键帧时间统一到原视频时间轴上，建立“原讲解—页面—操作—结果”证据链。后续的内容清理和画面重构都必须能回溯到这些证据。

## 2. 内容重构不是转写摘要

自然讲解中通常包含口头语、停顿、重复、临时试错和错误录制。Agent 不会直接把识别文字作为字幕，而是：

1. 清理没有教学价值的口语；
2. 识别被后续正确表达替代的内容；
3. 按任务闭环重新排列入口、动作和结果；
4. 为每个步骤保留原视频时间范围和证据；
5. 对无法确认的信息采用保守处理，不补充录屏中不存在的规则。

## 3. 新旁白会改变原有时间线

重新组织后的旁白通常与原讲解时长不同，因此不能只替换音轨。系统会读取每段配音的实际时长，再为对应画面计算目标时长和局部速度。

为了保证新手能够看清操作，普通步骤的加速上限默认是 `1.5x`。如果画面无法容纳解说，程序会中止并要求缩短文案或扩展画面范围，而不是强行生成难以理解的成片。

## 4. 操作提示必须依赖页面证据

点击提示使用相对于原画面的比例坐标，以适配不同分辨率。每个效果都带有起止时间，并与对应步骤的剪辑片段关联。

低置信度位置不应猜测。无法确认控件位置时，宁可不画点击框，也不输出错误指引。

## 5. 时间线正确不代表文件正确

最终视频还需要检查：

- 文件能否正常读取；
- 是否包含视频和音频轨道；
- 分辨率、编码和时长是否符合预期；
- 字幕是否遮挡操作区域；
- 关键帧是否展示了正确页面；
- 旁白、操作与结果是否保持顺序一致。

因此，最终验收针对导出的 MP4，而不是只检查配置文件或渲染日志。
